import { ElementStates } from '../types/element-states';

export interface ICharacter {
  character: string,
  state: ElementStates
}

export interface IArrObject {
  num: number
  state: ElementStates
}